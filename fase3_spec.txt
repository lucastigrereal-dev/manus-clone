FASE 3 — Missões Reais R2 (EVO-021 a EVO-030) → GO após Fase 2 estável\nFASE 4 — Vanguarda R2 (EVO-031 a EVO-040) → GO após Fase 3\nFASE 5 — Arquitetura R3 (EVO-041 a EVO-050) → GO após Fase 4\nIDEs    — Distribuídas nas fases por risk_level\n\n```\n\n📦 FASE 1 — QUICK WINS (EVO-001 a EVO-010) — INICIAR AGORA\nSetup inicial\n\n```powershell\nSet-Location C:\\Users\\lucas\\manus-clone\nnpm install cmdk react-hotkeys-hook @tanstack/react-query date-fns class-variance-authority swr idb-keyval framer-motion zustand\n\n```\n\nParalelismo interno\n\n```\n1A (paralelo): EVO-001, EVO-002, EVO-003, EVO-005, EVO-006\n1B (paralelo): EVO-004, EVO-007, EVO-009, EVO-010\nSEQUENCIAL (após 1A+1B): EVO-008\n\n```\n\nEVO-001 — Command Palette OMNIS\nDep: `cmdk` | Arquivo: `src/components/CommandPalette.tsx` | Atalho: Ctrl+K\n\n```typescript\ninterface OmnisCommand {\n  id: string\n  label: string\n  kind: 'route' | 'action' | 'factory' | 'external'\n  shortcut?: string\n  action: () => void\n}\nconst COMMANDS: OmnisCommand[] = [\n  { id: 'new-mission', label: 'Nova missão', kind: 'route', action: () => router.push('/missions/new') },\n  { id: 'akasha', label: 'Buscar no AKASHA', kind: 'route', action: () => router.push('/akasha') },\n  { id: 'app-factory', label: 'App Factory', kind: 'factory', action: () => setFactory('app') },\n  { id: 'dev-build', label: 'Dev Build', kind: 'factory', action: () => setFactory('dev') },\n  { id: 'instagram', label: 'Instagram', kind: 'factory', action: () => setFactory('instagram') },\n  { id: 'kratos', label: 'Abrir KRATOS', kind: 'external', action: () => window.open('http://localhost:5100') },\n  { id: 'focus', label: 'Modo Foco', kind: 'action', action: () => toggleFocusMode() },\n]\n\n```\n\nEVO-002 — Shortcuts globais\nDep: `react-hotkeys-hook` | Arquivo: `src/hooks/useOmnisShortcuts.ts`\n\n```typescript\nconst SHORTCUTS = {\n  'mod+k': () => setCommandPaletteOpen(true),\n  'mod+j': () => chatInputRef.current?.focus(),\n  'mod+enter': () => submitMessage(),\n  'mod+shift+f': () => toggleFocusMode(),\n  'escape': () => closeAllModals(),\n}\n// Chamar no layout root: useOmnisShortcuts()\n\n```\n\nEVO-003 — Modo foco anti-TDAH\nDep: `zustand` | Arquivo: `src/stores/uiStore.ts`\n\n```typescript\ninterface UiStore {\n  focusMode: boolean\n  toggleFocusMode: () => void\n  commandPaletteOpen: boolean\n  setCommandPaletteOpen: (open: boolean) => void\n  activeFactory: string | null\n  setActiveFactory: (f: string | null) => void\n}\n// Quando focusMode=true: esconde sidebar, esconde header metrics, fixa chat no centro\n// Botão toggle sempre visível mesmo em foco\n\n```\n\nEVO-004 — Histórico de missões na sidebar\nDep: `@tanstack/react-query`, `date-fns` | Arquivo: `src/components/MissionHistory.tsx`\n\n```typescript\n// Mock inicial — substituir por API real na Fase 2\nconst MOCK_MISSIONS = [\n  { id: 'mis_01JX001', title: 'Benchmark hotéis Natal', status: 'done', updatedAt: new Date(), factory: 'Research' },\n  { id: 'mis_01JX002', title: 'App Factory sprint 3', status: 'running', updatedAt: new Date(), factory: 'App' },\n  { id: 'mis_01JX003', title: 'Conteúdo Instagram Karina', status: 'failed', updatedAt: new Date(), factory: 'Content' },\n]\n// Status colors: done→emerald-400 | running→amber-400 animate-pulse | failed→red-400 | queued→zinc-400\n// Mostrar últimas 20. Clicar retoma contexto.\n\n```\n\nEVO-005 — Inline notifications no chat\nDep: `framer-motion` | Arquivo: `src/components/InlineToast.tsx`\n\n```typescript\ntype ToastKind = 'info' | 'success' | 'warning' | 'error'\ninterface InlineToast {\n  id: string\n  kind: ToastKind\n  message: string\n  autoDismiss?: number // ms, default 4000\n}\n// Posição: dentro do chat, abaixo do último bubble — NÃO fora do chat\n// Animação: AnimatePresence com slide-up\n\n```\n\nEVO-006 — Prompt chips por factory\nArquivo: `src/components/PromptChips.tsx`\n\n```typescript\nconst FACTORY_CHIPS = [\n  { label: '📱 Legenda Instagram', prompt: 'Crie uma legenda SEOgram para o perfil [perfil] sobre [tema]' },\n  { label: '🔬 Pesquisar benchmark', prompt: 'Pesquise benchmarks de [tema] no mercado brasileiro 2026' },\n  { label: '⚡ Nova missão App Factory', prompt: 'Quero criar um app que faz [descrição]. Monte o PRD com paralelismo interno.' },\n  { label: '💰 Análise de custo preflight', prompt: 'Analise o custo estimado desta missão antes de executar' },\n  { label: '🧠 Buscar no AKASHA', prompt: 'Busque no AKASHA memórias sobre [tema]' },\n  { label: '📊 Status dos serviços', prompt: 'Mostre o status atual de todos os serviços OMNIS' },\n  { label: '🎯 Próxima melhor ação', prompt: 'Com base no contexto atual, qual é o próximo melhor movimento?' },\n  { label: '📝 Gerar PRD', prompt: 'Gere um PRD completo com seção de paralelismo interno para: [ideia]' },\n]\n// Clicar preenche input — NÃO submete\n\n```\n\nEVO-007 — Cards de status de missão\nDep: `class-variance-authority` | Arquivo: `src/components/MissionCard.tsx`\n\n```typescript\nconst missionCard = cva('rounded-2xl border p-4 transition-all', {\n  variants: {\n    status: {\n      queued:  'border-zinc-500/20 bg-zinc-500/5',\n      running: 'border-amber-500/30 bg-amber-500/10 animate-pulse',\n      done:    'border-emerald-500/30 bg-emerald-500/10',\n      failed:  'border-red-500/30 bg-red-500/10',\n    }\n  }\n})\n// Campos: título, status badge, risk_level (R0/R1/R2/R3), ts, factory, custo estimado (quando disponível)\n\n```\n\nEVO-008 — Header vivo com health pills\nDep: `swr` | Arquivo: `src/components/HealthHeader.tsx`\n\n```typescript\n// src/app/api/health/route.ts (criar route handler)\nexport async function GET() {\n  return Response.json({\n    services: [\n      { name: 'OMNIS Core', status: 'ok', latencyMs: 45, port: 8000 },\n      { name: 'AKASHA',     status: 'ok', latencyMs: 12, port: 5432 },\n      { name: 'LiteLLM',   status: 'ok', latencyMs: 78, port: 4001 },\n      { name: 'Redis',      status: 'ok', latencyMs: 3,  port: 6379 },\n    ]\n  })\n}\n// Pills: ponto colorido + nome + latência\n// Verde <100ms | Amarelo 100-500ms | Vermelho >500ms ou offline\n// SWR refreshInterval: 5000ms\n// Fase 2: conectar nos serviços reais\n\n```\n\nEVO-009 — Draft autosave local-first\nDep: `idb-keyval` | Arquivo: `src/hooks/useDraftAutosave.ts`\n\n```typescript\n// Salvar após 500ms de inatividade (debounce)\n// Key: `draft:${conversationId}` ou `draft:global`\n// Restaurar ao montar componente de chat\n// Limpar ao submeter mensagem com sucesso\n// Fallback para localStorage se IndexedDB indisponível\n\n```\n\nEVO-010 — Empty states orientados\nArquivo: `src/components/EmptyState.tsx`\n\n```typescript\nconst EMPTY_STATES = {\n  no_missions: { icon: '🚀', title: 'Nenhuma missão ainda',\n    description: 'Digite um objetivo ou escolha um chip abaixo', cta: 'Nova missão' },\n  no_history: { icon: '📋', title: 'Histórico vazio',\n    description: 'Suas missões aparecem aqui após a primeira execução', cta: null },\n  no_search: { icon: '🔍', title: 'Nenhum resultado',\n    description: 'Tente outros termos ou busque no AKASHA', cta: 'Buscar no AKASHA' },\n  no_approvals: { icon: '✅', title: 'Nenhuma aprovação pendente',\n    description: 'Ótimo! Tudo está fluindo sem gates humanos', cta: null },\n}\n\n```\n\n📦 FASE 2 — INTEGRAÇÕES R1 (EVO-011 a EVO-020) — AGUARDAR GO\nEVO-011 — ORÁCULO no header\nKPIs resumidos no topo: ROI, missões/semana, custo total. Schema: `{ name, value, delta }`. Recharts.\nEVO-012 — Economic Brain preflight\nEstimativa de custo antes de executar. Schema: `{ estimatedCostUsd, decision: 'GO'|'VETO' }`. Endpoint: `POST /api/missions/preflight`.\nEVO-013 — AKASHA search inline\nBusca vetorial acionável no chat. Schema: `{ chunkId, score, content, source }`. Stack: pgvector + FastAPI + TanStack Query. Endpoint: `GET /api/akasha/search?q=`.\nEVO-014 — Health score em tempo real\nScore composto 0-100 com breakdown por serviço. SSE endpoint: `/api/health/stream`. Atualização <5s.\nEVO-015 — Projetos reais do OMNIS\nSubsti