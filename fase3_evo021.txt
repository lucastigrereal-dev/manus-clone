) + resumo + cache Redis de chunks semânticos. Schema: `{ recentTurns, summaryRef, memoryRefs[] }`. Endpoint: `POST /api/context-pack`.\n📦 FASE 3 — MISSÕES REAIS R2 (EVO-021 a EVO-030) — AGUARDAR GO\nEVO-021 — Launch mission via chat\nPrompt → mission_package → OMNIS Core. Schema: `{ objective, factory, risk_level, estimated_cost }`. Endpoint: `POST /api/missions`. Retorna `mission_id` ULID.\nEVO-022 — Mission stream SSE\n`EventSource('/api/missions/${id}/stream')`. Renderiza StepCard por evento. Ver componentes React base abaixo.\nEVO-023 — Wave progress cards\nCards por wave/step/substep. Schema: `{ id, label, progress: 0-100, status }`. Framer Motion progress bar.\nEVO-024 — Multi-agent visibility\nLanes por agente (Hermes/Vulcano/Muse/Aurora). Schema: `{ agent, status, steps[] }`. Cada lane atualiza via SSE.\nEVO-025 — Checkpoint approval inline\nMissão pausa em `event_type === 'HumanApprovalRequired'`. ApprovalCard bloqueia stream. GO retoma com `POST /api/approvals/${id}/decide`.\nEVO-026 — Rollback via chat\n`POST /api/missions/${id}/rollback`. Restaura checkpoint anterior. Schema: `{ missionId, checkpointId }`.\nEVO-027 — Mission replay\n`GET /api/missions/${id}/replay` retorna array de frames. Timeline navegável por timestamp.\nEVO-028 — Dry-run first\nPreview de plano + custo antes de executar ação R2+. Schema: `{ steps[], estimatedCostUsd, riskLevel }`. Sempre antes de ação externa.\nEVO-029 — Execution sidecar\nPainel lateral (react-resizable-panels) com tabs: steps / logs / artifacts. Sincronizado com stream SSE do chat.\nEVO-030 — Failure recovery playbooks\nMapear classe de erro → playbook. Schema: `{ errorClass, playbookId, suggestedAction }`. Ex: `RATELIMIT → retry_with_backoff`, `TIMEOUT → decompose_mission`.\n📦 FASE 4 — VANGUARDA R2 (EVO-031 a EVO-040) — AGUARDAR GO\nEVO-031 — Voice shell\nWeb Speech API + Whisper.cpp fallback. Schema: `{ transcript, confidence }`. Voz → input → submit automático.\nEVO-032 — Mission canvas visual\nReact Flow drag-drop de waves e agentes. Schema: `{ nodes[], edges[] }`. Editar plano e enviar ao Core.\nEVO-033 — Parallel mission tabs\nZustand store com array de tabs, cada uma com contexto isolado. Schema: `{ id, title, missionId, status }`.\nEVO-034 — What-if simulator\nSimular custo/tempo/risk antes do GO. Schema: `{ scenarios: [{ name, cost, time, risk }] }`. Endpoint: `POST /api/simulate`.\nEVO-035 — Economic dashboard inline\nRecharts acoplado ao chat. ROI, model_cost, custo/missão. Drilldown por fábrica.\nEVO-036 — Adaptive quick actions\nReordenar chips por frequência de uso. Schema: `{ action, usageCount, lastUsed, score }`. Persistido em Postgres.\nEVO-037 — Autopilot agenda\nExecutar agenda diária aprovada. Schema: `{ items: [{ time, mission }] }`. Requer R3 approval gate. Integrar com n8n `:5678`.\nEVO-038 — Live artifact pane\nRender parcial em tempo real: markdown, tabela, código, preview. Monaco Editor para código. react-markdown para texto.\nEVO-039 — Mission filmstrip\nFrames visuais por step como timeline de vídeo. Schema: `{ frames: [{ ts, label, screenshot? }] }`. Navegável por scrub.\nEVO-040 — Proactive blocker detection\nDetectar idle >7min ou loop de prompt similar. Schema: `{ signal: 'idle'|'loop', nextBestAction }`. Redis para tracking de inatividade.\n📦 FASE 5 — ARQUITETURA R3 (EVO-041 a EVO-050) — AGUARDAR GO FORMAL\nEVO-041 — OMNIS como MCP server\nCore expõe tools via MCP SDK TypeScript. Tools: `omnis.launch_mission`, `omnis.search_akasha`, `omnis.get_status`. Claude/Cursor/CLI consumindo como clientes externos.\nEVO-042 — Plugin marketplace para factories\nPlugins versionados (npm workspaces). Interface:\n\n```typescript\ninterface OmnisPlugin {\n  manifest: { name, version, factory }\n  register(ctx: PluginContext): void\n}\n\n```\n\nEVO-043 — Multi-user operators\nAuth.js + Casbin RBAC. Roles: `owner`, `approver`, `operator`, `viewer`. Schema: `{ userId, role, permissions[] }`.\nEVO-044 — Mobile shell PWA\nnext-pwa + Workbox. Tabs mobile: chat / approvals / health. Installable. Offline-lite para approvals pendentes.\nEVO-045 — OMNIS CLI espelhado\nInk + TypeScript. Comandos: `omnis mission launch`, `omnis stream ${id}`, `omnis approve ${id}`. Consome mesmo event log do shell.\nEVO-046 — Knowledge graph temporal\nGraphiti sobre AKASHA. Schema: `{ from, to, type, time }`. Navegar relações e episódios por entidade.\nEVO-047 — Policy-as-code runtime\nOpen Policy Agent + FastAPI. Toda ação passa por policy engine. Schema: `{ action, risk_level, decision: 'allow'|'deny' }`.\nEVO-048 — Self-improving prompt policies\nLangfuse → `LearningProposed` → review humana → promotion. Schema: `{ id, target: 'system_prompt'|'skill', diff }`. Pipeline com gate de aprovação.\nEVO-049 — Cross-client mission sync\nRedis Streams XREAD. Todos os clientes (shell/KRATOS/CLI) leem mesmo stream. Schema: `{ client, lastEventId }`. Uma única verdade operacional.\nEVO-050 — Factory OS composer\nReact Flow para encadear Research → Content → App → Commercial. Schema: `{ stages: [{ id, factory, inputs, outputs }] }`. Pipeline executa stages conectados via Core.\n💡 50 IDEIAS DE VANGUARDA (IDE-001 a IDE-050)\nCada IDE é uma evolução adicional. Implementar por risk_level e fase correspondente.\nIDE-001 R2 — Chat detecta travamento: idle>7min ou mesmo prompt 2x → sugere decomposição IDE-002 R1 — Missão gera próprio PRD antes do GO: `{ goal, acceptance[], risks[] }` IDE-003 R2 — Economic veto automático: threshold configurável, bloqueia missão cara demais IDE-004 R1 — Retomada automática de sessão: shell apresenta resumo ao reabrir IDE-005 R1 — Quick actions reordenadas por uso: Postgres + peso por frequência IDE-006 R3 — Autopilot diário: agenda aprovada roda automaticamente com n8n IDE-007 R1 — Split view vivo: chat à esquerda, artifact render à direita (react-resizable-panels) IDE-008 R2 — Mission replay cinematográfico: playback por step com controles de vídeo IDE-009 R1 — Kanban de missões SSE: colunas queued/running/blocked/done em tempo real (dnd-kit) IDE-010 R2 — Chat gera e roda testes: diff detectado → Vitest → relatório inline IDE-011 R2 — Ghost mode: shell propõe próximo melhor passo antes da pergunta IDE-012 R1 — Chat-to-KRATOS bridge: todo evento do chat aparece no cockpit KRATOS via Redis Streams IDE-013 R2 — Approval modify inline: editar payload antes de aprovar (React Hook Form) IDE-014 R1 — RAG por papel operacional: busca AKASHA filtrada por scope (research/content/sales) IDE-015 R1 — Session briefing automático: ao abrir, mostrar pendências + aprovações + briefing do dia IDE-016 R1 — Factory persona switching: mudar persona reconfigura tools e system prompt IDE-017 R2 — Chat over event log: conversa auditável e replayable via Postgres IDE-018 R1 — Semantic duplicate suppression: chunks redundantes do RAG suprimidos por hash semântico IDE-019 R1 — Model routing explainer: toda resposta mostra modelo usado + razão da escolha IDE-020 R1 — Confidence bar por resposta: barra de confiança operacional visível no bubble IDE-021 R1 — Mission decomposition wizard: prompt complexo → 3-7 subtasks com aprovação IDE-022 R1 — Tool-call badges: cada tool_use vira badge/card clicável no chat IDE-023 R1 — Context diff antes de responder: mostrar o que entrou/saiu do context pack IDE-024 R2 — Mission insurance mode: snapshot automático antes de qualquer ação R3 IDE-025 R1 — Next best factory: após Research sugerir Content ou Commercial encadeado IDE-026 R1 — Approval SLA countdown: cards de aprovação mostram tempo até expirar (date-fns) IDE-027 R2 — Failure tree view: erro mostra árvore causal upstream/downstream (React Flow) IDE-028 R1 — Prompt linting: UI aponta gaps de escopo antes de criar missão IDE-029 R1 — Plan-to-cost heatmap: heatmap por etapa mostrando hotspots de custo (Recharts) IDE-030 R1 — Memory freshness meter: cada context chip mostra quantos dias tem o chunk IDE-031 R1 — Agent handoff cards: troca de responsabilidade entre agentes vira card visual IDE-032 R1 — Mission brief to Notion: 1 clique exporta pesquisa para Notion via connector nativo IDE-033 R2 — Context pack versioning: cada resposta referencia `context_pack_id` auditável IDE-034 R2 — Approval simulation compare: approval 